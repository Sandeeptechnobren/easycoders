// Admin Attendance page — renders the shared AttendanceManagement component.
// The same component is rendered under /hr/attendance for the HR role.
export { default } from '@/components/AttendanceManagement';
