// HR Tickets page — renders the shared TicketManagement component.
// The same component is rendered under /admin/tickets for the admin role
// (and any user granted manage_queries via per-user override).
export { default } from '@/components/TicketManagement';
