// HR Attendance page — renders the shared AttendanceManagement component.
// The same component is rendered under /admin/attendance for the admin role.
export { default } from '@/components/AttendanceManagement';
